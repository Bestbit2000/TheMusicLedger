{
  "buildCommand": "npm install && cd server && npm install",
  "functions": {
    "api/[...slug].js": {
      "includeFiles": "public/**"
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/[...slug]"
    }
  ]
}
